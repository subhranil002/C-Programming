#include <stdio.h>

int main() {
    int a = 5;
    int b = 50;
    int c = 500;

    int avg = (a + b + c) / 3;

    printf("The average of %d, %d, and %d is: %d", a, b, c, avg); // Output: The average of 5, 50, and 500 is: 185

    return 0;
}
