#include <stdio.h>

int main() {
    int number = 25;   // Declares an integer variable named "number" and initializes it to 25
    char star = '*';   // Declares a character variable named "star" and initializes it to the character '*'
    float pi = 3.14;   // Declares a floating-point variable named "pi" and initializes it to 3.14
    int _age = 36;     // Declares an integer variable named "_age" and initializes it to 36
    
    return 0;  // Indicates successful termination of the program
}
