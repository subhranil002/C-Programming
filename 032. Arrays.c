#include <stdio.h>

int main() {
    int arr[5] = {1, 2, 3, 4, 5};

    // Print the first element of the array
    printf("%d \n", arr[0]); // Output: 1

    // Print the last element of the array
    printf("%d \n", arr[4]); // Output: 5

    return 0;
}